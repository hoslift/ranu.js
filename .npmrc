# Ranu.js monorepo pnpm configuration
# Strict dependency resolution — prevents hoisting undeclared dependencies
node-linker=node-modules
strict-peer-dependencies=false
auto-install-peers=true

# Workspace protocol support
prefer-workspace-packages=true

# Security: always verify integrity
verify-store-integrity=true

# Prevent accidental publication of private packages
publish-branch=main
