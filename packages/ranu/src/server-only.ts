/**
 * Ranu.js/server-only
 *
 * Import this module to mark a file as server-only.
 * If imported in a client bundle, the build will fail.
 *
 * Phase 0 skeleton — enforcement will be implemented in Phase 12.
 */

// Intentionally empty — the presence of this import is what matters
export {};
