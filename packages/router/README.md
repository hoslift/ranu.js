# @ranu/router

Internal Ranu.js package. Not public application API.

Owns filesystem route discovery, segment parsing, route tree, precedence, collision detection, matching, and route manifest generation.
