/**
 * @ranu/cli
 *
 * CLI argument parsing, command definitions, project discovery, orchestration.
 * Internal package — not public application API.
 *
 * Phase 0 skeleton — full implementation in Phase 22.
 */

export const CLI_VERSION = '0.0.0';
