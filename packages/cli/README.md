# @ranu/cli

Internal Ranu.js package. Not public application API.

Owns CLI argument parsing, command definitions, project discovery, and orchestration.

The public `Ranu.js` executable is provided by the `Ranu.js` package.
